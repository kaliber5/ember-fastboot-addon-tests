/* jshint node: true */
var setup = require('ember-fastboot-addon-tests').setupTestsForFastboot;

setup({
  // set custom options here if needed:
  // emberVersion: '~2.8.0',
  // emberDataVersion: '~2.8.0',
  // timeout: 600000
});